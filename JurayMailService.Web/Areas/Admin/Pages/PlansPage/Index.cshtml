@page
@model JurayMailService.Web.Areas.Admin.Pages.PlansPage.IndexModel
@{
}
<div class="card">
    <div class="card-header">
        <h3 class="card-title">PLAN</h3>


        <a asp-page="./Add" class="btn bg-gradient-warning btn-sm" style="float:right;">New Plan</a>

    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <div class="table-responsive">
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>S/N</th>
                        <th>
                            Name
                        </th>

                        <th>Limit</th>
                        <th>Price</th>

                        <th>Action</th>

                    </tr>
                </thead>

                @{
                    int sn = 0;
                }
                <tbody>
                    @foreach (var item in Model.Plans)
                    {
                        <tr>

                            <td>
                                @{
                                    sn++;
                                }
                                @sn
                            </td>
                            <td>
                                <a asp-page="./Info" asp-route-id="@item.Id">
                                    @Html.DisplayFor(modelItem => item.Name)

                                </a>
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.MonthlyLimit)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Price)
                            </td>

                            <td>
                                <div class="btn-group">
                                    <button type="button" class="btn btn-success">Action</button>

                                    <div class="btn-group">
                                        <button type="button" class="btn btn-success dropdown-toggle dropdown-icon" data-toggle="dropdown" aria-expanded="false">
                                        </button>
                                        <div class="dropdown-menu" style="">

                                            <a class="dropdown-item" asp-page="./Update" asp-route-id="@item.Id">Edit</a>
                                            <a class="dropdown-item" asp-page="./Info" asp-route-id="@item.Id">Info</a>

                                        </div>
                                    </div>
                                </div>


                            </td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>